# وثيقة المتطلبات

## 1. نظرة عامة على التطبيق

### 1.1 اسم التطبيق
Vodafone Fakka Premium

### 1.2 المالك
Nader Akram

### 1.3 الإصدار
v3.0.38

### 1.4 الوصف
منصة ويب احترافية لشحن كروت الفكة مع نظام اشتراكات وأكواد ترخيص، مصممة بواجهة عربية فاخرة وتدعم التكامل المستقبلي مع محرك شحن خارجي. تتضمن مركز شبكات متعدد لعرض خدمات Vodafone وOrange وEtisalat وWE، مع نظام إدارة كروت ديناميكي كامل وتحسينات أداء شاملة. تشمل المرحلة الجديدة إعادة بناء لوحة الإدارة بالكامل بصفحات كاملة احترافية، بالإضافة إلى نظام تقييم الكروت واقتراح التعديلات.

### 1.5 المبادئ الأساسية
- جميع البيانات ديناميكية (الكروت، الأسعار، الوحدات، الحالات)
- لا توجد بيانات ثابتة أو افتراضية
- النظام جاهز للربط مع API خارجي أو Recharge Engine
- تصميم Premium حديث بنمط Dark Luxury UI
- دعم كامل للغة العربية RTL
- Mobile First و Responsive
- Offline-First مع مزامنة ذكية
- منع التكرار في جميع العمليات
- لوحة إدارة احترافية بصفحات كاملة بدون Popup/Modal

### 1.6 الألوان
- Background: #0B0B14
- Primary: #E60000
- Secondary: #FFFFFF
- Accent: #FF3B3B
- Cards: #151523
- Borders: rgba(255,255,255,0.08)

### 1.7 خصائص التصميم
- Glassmorphism خفيف
- Shadows احترافية
- Animations ناعمة وسريعة
- Glow Effects و Border Animations
- Smooth Transitions
- Skeleton Loading
- Sticky Header و Sticky Action Bar

---

## 2. المستخدمون وسيناريوهات الاستخدام

### 2.1 المستخدمون المستهدفون
- المستخدمون النهائيون: أفراد يحتاجون لشحن كروت الفكة واستعراض خدمات الشبكات
- المسؤولون (Admin): إدارة المستخدمين والاشتراكات والأكواد والكروت والإصدارات والميزات
- المسؤولون الرئيسيون (Super Admin): صلاحيات كاملة على النظام

### 2.2 السيناريوهات الأساسية
- تفعيل الاشتراك باستخدام كود تفعيل
- شحن كروت الفكة بشكل متكرر
- استعراض خدمات الشبكات المختلفة
- حفظ الأرقام المفضلة للشحن السريع
- مراجعة سجل العمليات السابقة
- مراجعة الإحصائيات التفصيلية
- تقييم الكروت واقتراح تعديلات على بياناتها (جديد)
- إدارة الاشتراكات والأكواد والكروت (للمسؤولين)
- إدارة الأصول المرئية الديناميكية (للمسؤولين)
- حذف المستخدمين نهائياً (للمسؤولين)
- مراقبة المستخدمين والإصدارات في الوقت الفعلي (للمسؤولين)
- إدارة الميزات والإصدارات بدون تحديث APK (للمسؤولين)
- تنفيذ إجراءات متقدمة على حسابات المستخدمين (للمسؤولين)
- مراجعة تقييمات واقتراحات المستخدمين على الكروت (للمسؤولين، جديد)

---

## 3. هيكل الصفحات والوظائف

### 3.1 هيكل الصفحات

```
Vodafone Fakka Premium
├── شاشة البداية (Splash Screen)
├── نظام التفعيل
├── الصفحة الرئيسية
├── صفحة الشبكات
│   ├── صفحة Vodafone
│   ├── صفحة Orange
│   ├── صفحة Etisalat
│   └── صفحة WE
├── المفضلة
├── سجل العمليات
├── الإحصائيات
├── الإشعارات
├── الإعدادات
│   ├── الملف الشخصي
│   ├── حالة الاشتراك
│   ├── الدعم الفني
│   ├── تسجيل الخروج
│   └── حول التطبيق
└── لوحة الأدمن (Admin Dashboard)
    ├── Users
    │   ├── /admin/users — قائمة المستخدمين
    │   ├── /admin/users/{id} — تفاصيل المستخدم
    │   ├── /admin/users/{id}/actions — إجراءات المستخدم
    │   ├── /admin/users/{id}/subscription — إدارة الاشتراك
    │   └── /admin/users/{id}/operations — تعديل العمليات
    ├── Subscriptions
    ├── License Keys
    ├── Product Config
    ├── Recharge Engine
    ├── Operations
    ├── Logs
    ├── Notifications
    ├── Settings
    ├── Asset Management
    ├── Application Version Center
    ├── Live Monitoring
    ├── Feature Management
    └── Card Feedback (جديد)
```

### 3.2 شاشة البداية (Splash Screen)

#### الوظيفة
- عرض شعار فاخر للتطبيق (ديناميكي قابل للتعديل من لوحة الأدمن)
- عرض النصوص: Vodafone Fakka Premium / Powered By / Nader Akram
- تشغيل Animation احترافي أثناء التحميل
- تحميل البيانات الأساسية من التخزين المحلي أولاً
- الانتقال التلقائي للصفحة التالية بعد اكتمال التحميل

### 3.3 نظام التفعيل

#### الوظيفة
- يظهر عند أول دخول للمستخدم أو عند انتهاء الاشتراك
- عرض كارت فاخر بعنوان: تفعيل الاشتراك
- وصف: أدخل كود التفعيل الخاص باشتراكك لتفعيل الحساب
- حقل إدخال: كود التفعيل
- زر: تفعيل الاشتراك
- زر واتساب مباشر لرقم: 01222692182
- رسالة واتساب تلقائية: السلام عليكم أرغب في تفعيل اشتراك Vodafone Fakka Premium برجاء إرسال تفاصيل الدفع.
- بعد التفعيل الناجح: عرض حالة الاشتراك، تاريخ التفعيل، تاريخ الانتهاء، الأيام المتبقية
- جميع البيانات ديناميكية من الخادم

### 3.4 الصفحة الرئيسية

#### الوظيفة
- عرض شعار الهيدر في أعلى اليمين (ديناميكي)
- عرض كارت ترحيب فاخر يحتوي على: أيقونة الترحيب (ديناميكية)، اسم المستخدم، حالة الاشتراك، تاريخ التفعيل، تاريخ الانتهاء، الأيام المتبقية، إجمالي العمليات، إجمالي الكروت، إجمالي المبالغ، آخر عملية
- عرض بانر رئيسي (ديناميكي)
- عرض بانرات ترويجية (ديناميكية)
- عرض كارت Premium: عروض باقي الشبكات
- جميع البيانات ديناميكية من الخادم
- روابط سريعة للوظائف الرئيسية
- إخفاء جميع أدوات المطورين والتصحيح من واجهة المستخدم العادي
- تحميل البيانات من التخزين المحلي أولاً ثم المزامنة

### 3.5 صفحة الشبكات

#### الوظيفة
- عرض 4 بطاقات كبيرة Premium للشبكات: Vodafone، Orange، Etisalat، WE
- كل بطاقة تحتوي على: شعار الشبكة الرسمي (ديناميكي)، لون الشبكة الرسمي في التصميم، اسم الشبكة، وصف احترافي، عدد الخدمات المتاحة، سهم للدخول
- تصميم مختلف لكل بطاقة يعكس هوية الشبكة
- Animations ناعمة عند التنقل
- عند الضغط على أي بطاقة: الانتقال إلى صفحة الشبكة المحددة
- تحميل الشعارات من التخزين المحلي

### 3.6 صفحة Vodafone

#### الوظيفة
- Header كبير يحتوي على: شعار Vodafone الرسمي، اسم الشبكة: Vodafone، وصف احترافي
- قسم الخدمات: عرض رسالة تحت التطوير، نص احترافي: الخدمات قيد التطوير وستكون متاحة قريباً
- زر العودة للشبكات
- البنية جاهزة لإضافة مستقبلاً: فكة، دقائق، ميجابايت، باقات، كروت، عروض، خدمات

### 3.7 صفحة Orange

#### الوظيفة
- Header كبير يحتوي على: شعار Orange الرسمي، اسم الشبكة: Orange، وصف احترافي
- قسم الخدمات: عرض رسالة تحت التطوير
- زر العودة للشبكات
- البنية جاهزة لإضافة مستقبلاً: فكة، دقائق، ميجابايت، باقات، كروت، عروض، خدمات

### 3.8 صفحة Etisalat

#### الوظيفة
- Header كبير يحتوي على: شعار Etisalat الرسمي، اسم الشبكة: Etisalat، وصف احترافي
- قسم الخدمات: عرض رسالة تحت التطوير
- زر العودة للشبكات
- البنية جاهزة لإضافة مستقبلاً: فكة، دقائق، ميجابايت، باقات، كروت، عروض، خدمات

### 3.9 صفحة WE

#### الوظيفة
- Header كبير يحتوي على: شعار WE الرسمي، اسم الشبكة: WE، وصف احترافي
- قسم الخدمات: عرض رسالة تحت التطوير
- زر العودة للشبكات
- البنية جاهزة لإضافة مستقبلاً: فكة، دقائق، ميجابايت، باقات، كروت، عروض، خدمات

### 3.10 المفضلة

#### الوظيفة
- عرض قائمة الأرقام المحفوظة
- كل عنصر يحتوي على: اسم مخصص (اختياري)، رقم الهاتف
- إضافة رقم جديد للمفضلة
- تعديل بيانات رقم محفوظ
- حذف رقم من المفضلة
- الشحن السريع من المفضلة

### 3.11 سجل العمليات

#### الوظيفة
- عرض قائمة العمليات السابقة
- كل عملية تحتوي على: رقم العملية، رقم الهاتف، الفئة، المبلغ، نوع الكارت المستخدم، وقت العملية، حالة العملية (نجاح أو فشل)
- ترتيب العمليات من الأحدث للأقدم
- زر Copy بجانب رقم العملية
- زر تقييم الكارت / اقتراح تعديل (جديد): يظهر بجانب كل عملية ناجحة إذا كانت الميزة مفعلة

### 3.12 الإحصائيات

#### الوظيفة
- عرض إحصائيات شاملة: إجمالي العمليات، إجمالي الكروت، إجمالي المبالغ، عدد الأرقام المستخدمة
- عرض إحصائيات فترات زمنية: عمليات اليوم، عمليات الأسبوع، عمليات الشهر
- عرض آخر عملية
- عرض Charts احترافية ديناميكية
- جميع البيانات ديناميكية من الخادم

### 3.13 الإشعارات

#### الوظيفة
- عرض قائمة الإشعارات الخاصة بالمستخدم فقط
- أنواع الإشعارات: تنبيهات تجديد الاشتراك، تنبيهات النظام، إشعارات العمليات
- عرض وقت كل إشعار
- تمييز الإشعارات غير المقروءة
- منع تكرار الإشعارات
- حذف تلقائي للإشعارات الأقدم من 30 يوماً

### 3.14 الإعدادات

#### 3.14.1 الملف الشخصي
- عرض بيانات المستخدم
- تعديل البيانات الشخصية

#### 3.14.2 حالة الاشتراك
- عرض تفاصيل الاشتراك الحالي
- تاريخ التفعيل
- تاريخ الانتهاء
- الأيام المتبقية

#### 3.14.3 الدعم الفني
- معلومات التواصل مع الدعم
- إرسال استفسار أو مشكلة

#### 3.14.4 تسجيل الخروج
- تسجيل خروج المستخدم من الحساب

#### 3.14.5 حول التطبيق
- معلومات عن التطبيق
- رقم الإصدار: v3.0.38
- Powered By Nader Akram
- All Rights Reserved

### 3.15 نافذة تقييم الكارت / اقتراح تعديل (جديد)

#### الوظيفة
- نافذة منبثقة (Popup) تظهر عند الضغط على زر تقييم الكارت
- عنوان النافذة: تقييم الكارت / اقتراح تعديل
- عرض معلومات العملية: رقم العملية، نوع الكارت، التاريخ
- حقول الإدخال:
  - عدد الوحدات الفعلية (حقل رقمي)
  - السعر المخصوم الفعلي من فودافون كاش (حقل رقمي)
  - أيام الصلاحية الفعلية (حقل رقمي)
- زر رفع سكرين شوت (اختياري):
  - يسمح برفع صورة واحدة
  - عرض الصورة بشكل مصغر بعد الرفع
  - إمكانية حذف الصورة قبل الإرسال
- زر إرسال التقييم
- زر إلغاء
- تصميم النافذة يتبع نفس الهوية البصرية للتطبيق

### 3.16 لوحة الأدمن (Admin Dashboard)

#### 3.16.1 Users

##### 3.16.1.1 قائمة المستخدمين (/admin/users)
- عرض قائمة المستخدمين
- بحث وفلترة المستخدمين
- Sorting حسب: تاريخ التسجيل، آخر نشاط، حالة الاشتراك
- Pagination
- عرض معلومات مختصرة لكل مستخدم: الصورة، الاسم، User ID، الحالة، نوع الحساب، آخر نشاط
- زر للانتقال إلى صفحة تفاصيل المستخدم

##### 3.16.1.2 صفحة تفاصيل المستخدم (/admin/users/{id})
- Header واضح يحتوي على: اسم المستخدم، User ID، زر العودة
- صورة المستخدم
- الحالة: نشط، معلق، محظور
- نوع الحساب: User، Admin، Super Admin
- الإصدار الحالي للمستخدم
- آخر إصدار متاح
- آخر Login
- آخر Activity
- معلومات الاشتراك: حالة الاشتراك، تاريخ التفعيل، تاريخ الانتهاء، الأيام المتبقية، الكود المستخدم
- الإحصائيات: إجمالي العمليات، إجمالي الكروت، إجمالي المبالغ، عدد الأرقام المفضلة
- العمليات الأخيرة (آخر 5 عمليات)
- سجل النشاط (آخر 10 أنشطة)
- سجل التعديلات (جميع التعديلات التي تمت على الحساب)
- سجل الاشتراك (تاريخ جميع الاشتراكات والتجديدات)
- سجل الحظر (إذا تم حظر الحساب سابقاً)
- سجل الأكواد (جميع الأكواد المستخدمة)
- سجل كامل لأي إجراء تم على المستخدم
- أزرار Copy بجانب: اسم المستخدم، User ID، رقم الهاتف، البريد
- أزرار سريعة: عرض الإجراءات، إدارة الاشتراك، تعديل العمليات

##### 3.16.1.3 صفحة إجراءات المستخدم (/admin/users/{id}/actions)
- Header: إجراءات المستخدم، اسم المستخدم، زر العودة
- الإجراءات مقسمة إلى مجموعات:

**Subscription:**
- تجديد الاشتراك
- تمديد الاشتراك
- إلغاء الاشتراك
- تغيير الكود

**Account:**
- تعليق الحساب
- إعادة تفعيل الحساب
- حظر الحساب
- رفع الحظر

**Security:**
- Logout All Devices
- Reset Session
- Reset Device
- Reset Limits

**Operations:**
- تعديل العمليات
- تصفير العمليات
- تعديل الحد اليومي

**Logs:**
- سجل العمليات
- سجل التعديلات
- سجل تسجيل الدخول
- سجل الأخطاء

- كل إجراء يحتوي على: وصف الإجراء، زر التنفيذ، Confirmation Dialog
- تسجيل كل إجراء في سجل التعديلات

##### 3.16.1.4 صفحة إدارة الاشتراك (/admin/users/{id}/subscription)
- Header: إدارة الاشتراك، اسم المستخدم، زر العودة
- عرض تفاصيل الاشتراك الحالي
- نموذج تجديد الاشتراك: اختيار مدة الاشتراك، اختيار كود جديد (اختياري)، زر تجديد
- نموذج تمديد الاشتراك: إدخال عدد الأيام، زر تمديد
- نموذج إلغاء الاشتراك: سبب الإلغاء، Confirmation، زر إلغاء
- سجل جميع التجديدات والتمديدات السابقة

##### 3.16.1.5 صفحة تعديل العمليات (/admin/users/{id}/operations)
- Header: تعديل العمليات، اسم المستخدم، زر العودة
- عرض قائمة جميع عمليات المستخدم
- لكل عملية: رقم العملية، رقم الهاتف، الفئة، المبلغ، الحالة، التاريخ، زر تعديل
- نموذج تعديل العملية: تعديل الحالة، تعديل المبلغ، إضافة ملاحظة، زر حفظ
- زر تصفير جميع العمليات (مع Confirmation)
- تسجيل كل تعديل في سجل التعديلات

#### 3.16.2 Subscriptions
- عرض قائمة الاشتراكات
- حالة كل اشتراك (نشط، منتهي، معلق)
- تاريخ التفعيل والانتهاء
- تجديد أو تعديل اشتراك

#### 3.16.3 License Keys
- إنشاء كود تفعيل جديد
- تحديد مدة الاشتراك للكود
- عرض قائمة الأكواد
- حالة كل كود (نشط، مستخدم، معطل)
- عرض المستخدم المرتبط بالكود
- تعطيل كود
- حذف كود

#### 3.16.4 Product Config

##### الوظيفة
- إدارة إعدادات جميع الكروت من جدول product_config في Supabase
- عرض قائمة جميع الكروت
- لكل كارت:
  - تفعيل/إيقاف (ON/OFF)
  - إظهار/إخفاء
  - الحالة: active، maintenance، development، unavailable، disabled_execution
  - السعر
  - الوحدات
  - مدة الصلاحية
  - الربح
  - الترتيب
  - إعدادات API خاصة
- تعديل أي إعداد ينعكس فوراً على جميع المستخدمين بدون تحديث APK
- بحث وفلترة الكروت
- تصدير إعدادات الكروت

#### 3.16.5 Recharge Engine
- قسم فارغ مخصص للربط المستقبلي
- لا يحتوي على أي API أو منطق شحن
- جاهز لاستقبال التكامل مع سكربت الشحن الخارجي

#### 3.16.6 Operations
- عرض جميع عمليات الشحن في النظام
- فلترة حسب المستخدم، التاريخ، الحالة
- تصدير التقارير
- زر Copy بجانب رقم العملية وTransaction ID

#### 3.16.7 Logs
- عرض سجلات النظام
- تسجيل جميع الأحداث المهمة
- فلترة حسب النوع والتاريخ

#### 3.16.8 Notifications
- إرسال إشعارات للمستخدمين
- إدارة قوالب الإشعارات
- عرض سجل الإشعارات المرسلة
- منع تكرار الإشعارات

#### 3.16.9 Settings
- إعدادات عامة للنظام
- إدارة الصلاحيات
- تخصيص الواجهة

#### 3.16.10 Asset Management

##### الوظيفة
- إدارة الأصول المرئية الديناميكية للتطبيق
- أقسام الأصول: شعار التطبيق، شعار الهيدر، شعار شاشة البداية، أيقونة كارت الترحيب، البانر الرئيسي، البانرات الترويجية، صور العروض، صور الفئات، شعارات الشبكات (Vodafone، Orange، Etisalat، WE)
- وظائف كل قسم: عرض الصورة الحالية، رفع صورة جديدة، معاينة الصورة قبل الحفظ، حذف الصورة، استعادة الصورة الافتراضية
- قواعد الرفع: الصيغ المدعومة (PNG, JPG, JPEG, WEBP, SVG)، الحد الأقصى لحجم الملف (10 MB)، التخزين (Supabase Storage)
- التحديث الفوري: عند رفع صورة جديدة، رفع الملف إلى Supabase Storage، تحديث قاعدة البيانات، تحديث فوري لجميع المستخدمين، لا حاجة لتحديث APK

#### 3.16.11 Application Version Center

##### الوظيفة
- إدارة إصدارات التطبيق
- عرض معلومات الإصدارات:
  - Current Version: الإصدار الحالي المستخدم من معظم المستخدمين
  - Latest Version: آخر إصدار متاح
  - Minimum Version: الحد الأدنى المسموح
  - Blocked Versions: الإصدارات المحظورة
  - Recommended Version: الإصدار الموصى به
  - Server Version: إصدار الخادم
  - Build Number: رقم البناء
  - Version Code: كود الإصدار
  - Version Name: اسم الإصدار
  - Release Date: تاريخ الإصدار
  - Compatibility: التوافق مع الأجهزة
  - Force Update Status: حالة التحديث الإجباري
- عرض عدد المستخدمين على كل إصدار
- إدارة Force Update: تفعيل/إيقاف التحديث الإجباري، تحديد الإصدار المطلوب
- حظر إصدارات معينة
- تحديد الحد الأدنى المسموح

#### 3.16.12 Live Monitoring

##### الوظيفة
- مراقبة المستخدمين في الوقت الفعلي
- عرض:
  - عدد المستخدمين Online
  - عدد المستخدمين Offline
  - الإصدار المستخدم لكل مستخدم
  - عدد مستخدمي كل إصدار
  - عدد الأجهزة المتصلة
  - آخر اتصال لكل مستخدم
  - آخر تحديث
  - آخر Sync
  - آخر API Request
  - آخر Runtime Config
  - آخر Feature Flags
- تحديث تلقائي كل 10 ثوانٍ
- فلترة حسب: الحالة، الإصدار، نوع الجهاز
- عرض تفاصيل كل مستخدم عند الضغط عليه

#### 3.16.13 Feature Management

##### الوظيفة
- إدارة الميزات بدون تحديث APK
- تشغيل/إيقاف أي ميزة:
  - الكروت (كل كارت على حدة)
  - الخدمات (كل خدمة على حدة)
  - الصفحات (كل صفحة على حدة)
  - الأدوات (كل أداة على حدة)
  - API معين
  - زر معين
  - أي جزء داخل التطبيق
  - ميزة تقييم الكروت (جديد)
- عرض قائمة جميع الميزات
- لكل ميزة:
  - الاسم
  - الوصف
  - الحالة (مفعل/معطل)
  - تاريخ آخر تعديل
  - المسؤول الذي عدّل
  - عدد المستخدمين المتأثرين
- تفعيل/إيقاف فوري
- يُطبَّق على جميع المستخدمين المتصلين فوراً
- تسجيل كل تعديل في سجل النظام

#### 3.16.14 Card Feedback (جديد)

##### الوظيفة
- عرض قائمة جميع تقييمات واقتراحات المستخدمين على الكروت
- لكل تقييم:
  - اسم المستخدم
  - User ID
  - رقم العملية
  - نوع الكارت
  - تاريخ العملية
  - تاريخ التقييم
  - البيانات المقترحة:
    - عدد الوحدات الفعلية
    - السعر المخصوم الفعلي
    - أيام الصلاحية الفعلية
  - السكرين شوت (إن وجد)
  - حالة التقييم: جديد، قيد المراجعة، تم التطبيق، مرفوض
- فلترة حسب: نوع الكارت، حالة التقييم، التاريخ، المستخدم
- Sorting حسب: تاريخ التقييم، نوع الكارت
- Pagination
- عرض السكرين شوت بحجم كامل عند الضغط عليه
- تغيير حالة التقييم
- إضافة ملاحظات Admin على التقييم
- حذف تقييم
- تصدير التقييمات

### 3.17 Bottom Navigation

#### الوظيفة
- تبويب الرئيسية
- تبويب الشبكات (أيقونة أبراج اتصال)
- تبويب المفضلة
- تبويب السجل
- تبويب الإحصائيات

### 3.18 Footer (في جميع الصفحات)

#### الوظيفة
- عرض في أسفل كل صفحة: Vodafone Fakka Premium، © 2026 Nader Akram، جميع الحقوق محفوظة

---

## 4. القواعد والمنطق

### 4.1 نظام الصلاحيات
- User: صلاحيات المستخدم العادي (الشبكات، المفضلة، السجل، الإحصائيات، الإعدادات)
- Admin: صلاحيات إدارية (إدارة المستخدمين، الاشتراكات، الأكواد، الكروت، العمليات، الأصول المرئية، الإصدارات، الميزات، المراقبة، تقييمات الكروت)
- Super Admin: صلاحيات كاملة على جميع أقسام النظام

### 4.2 منطق التفعيل
- عند إدخال كود تفعيل صحيح: تفعيل الاشتراك، تسجيل تاريخ التفعيل، حساب تاريخ الانتهاء بناءً على مدة الكود، ربط الكود بالمستخدم، تحديث حالة الكود إلى مستخدم
- عند إدخال كود خاطئ أو مستخدم مسبقاً: عرض رسالة خطأ، عدم السماح بالمتابعة

### 4.3 منطق الشحن
- التحقق من صلاحية الاشتراك قبل السماح بالشحن
- التحقق من صحة رقم الهاتف والرقم السري
- التحقق من اختيار الكارت
- التحقق من حالة الكارت من product_config
- إذا كان الكارت (maintenance/development/unavailable): لا يُرسل أي API، يظهر Dialog: تم إيقاف هذا المنتج مؤقتاً، وسيعود للعمل فور توفره.
- إذا كان disabled_execution: الكارت ظاهر لكن التنفيذ معطّل بنفس الـ Dialog
- عرض رسالة تنبيه قبل التنفيذ: يرجى التأكد قبل التنفيذ من عدم وجود أي مبلغ مستحق أو استلاف أو نوتة على الخط المرسل إليه، لأن ذلك قد يؤدي إلى عدم إتمام العملية بنجاح.
- منع إرسال الطلب مرتين (Request Deduplication)
- إرسال طلب الشحن (سيتم ربطه لاحقاً مع Recharge Engine)
- انتظار النتيجة النهائية قبل عرض الرسالة
- تسجيل العملية في السجل
- عرض نتيجة العملية (نجاح أو فشل)
- إذا نجحت العملية: لا تظهر رسالة فشل أبداً، عرض زر تقييم الكارت (إذا كانت الميزة مفعلة)
- نظام الشحن الحالي يعمل بشكل صحيح ولا يجب تعديله

### 4.4 منطق الاشتراكات
- حساب الأيام المتبقية تلقائياً
- عند انتهاء الاشتراك: تحديث حالة الاشتراك إلى منتهي، منع المستخدم من الشحن، عرض شاشة التفعيل، إرسال إشعار تجديد

### 4.5 منطق الأكواد
- كل كود يستخدم مرة واحدة فقط
- الكود المعطل لا يمكن استخدامه
- الكود المحذوف لا يظهر في النظام

### 4.6 منطق البيانات الديناميكية
- جميع الكروت يتم تحميلها من product_config في Supabase
- جميع إعدادات الكروت ديناميكية
- لا توجد بيانات ثابتة في الكود
- النظام يعرض فقط ما يتم تحميله
- في حالة عدم توفر بيانات: عرض رسالة لا توجد كروت متاحة حالياً

### 4.7 منطق الإحصائيات
- حساب جميع الإحصائيات ديناميكياً من قاعدة البيانات
- تحديث الإحصائيات تلقائياً عند إضافة عملية جديدة
- عرض Charts ديناميكية بناءً على البيانات الفعلية

### 4.8 منطق إخفاء أدوات المطورين
- إخفاء جميع أدوات المطورين من واجهة المستخدم العادي
- لا يتم حذف الكود أو إزالة الوظائف
- يتم إخفاء العناصر من واجهة المستخدم فقط
- تستمر جميع الوظائف في العمل داخلياً
- المسؤولون (Admin/Super Admin) يمكنهم الوصول لهذه الأدوات عند الحاجة

### 4.9 منطق الأصول المرئية الديناميكية
- مواقع الأصول المرئية: شعار الهيدر في أعلى اليمين، أيقونة كارت الترحيب، شعار شاشة البداية، شعارات الشبكات في صفحة الشبكات وصفحات الشبكات الفردية
- عملية التحديث: Admin يرفع صورة جديدة، رفع الملف إلى Supabase Storage، تحديث قاعدة البيانات بالرابط الجديد، تحديث فوري لجميع المستخدمين، لا حاجة لتحديث APK
- التحقق من الصيغة والحجم: التحقق من صيغة الملف (PNG, JPG, JPEG, WEBP, SVG)، التحقق من حجم الملف (أقل من 10 MB)، رفض الملفات غير المطابقة

### 4.10 منطق صفحة الشبكات
- عرض 4 بطاقات للشبكات بتصميم Premium
- كل بطاقة تعرض شعار الشبكة الديناميكي من Asset Management
- تطبيق لون الشبكة الرسمي في التصميم
- عرض عدد الخدمات (حالياً 0 لجميع الشبكات)
- عند الضغط على أي بطاقة: الانتقال إلى صفحة الشبكة المحددة
- Animations ناعمة بدون تهنيج
- بدون إعادة تحميل الصفحة

### 4.11 منطق صفحات الشبكات الفردية
- البنية الحالية: عرض Header بشعار الشبكة واسمها ووصف احترافي، عرض قسم الخدمات برسالة تحت التطوير، زر العودة للشبكات
- البنية المستقبلية (Modular): جاهزة لإضافة أقسام (فكة، دقائق، ميجابايت، باقات، كروت، عروض، خدمات)

### 4.12 منطق Offline-First
- تخزين البيانات الثابتة محلياً باستخدام Capacitor Preferences: إعدادات التطبيق، الكروت، الصور، الأيقونات
- فتح التطبيق من التخزين المحلي أولاً
- مزامنة ذكية في الخلفية
- لا skeleton طويل، لا صور مكسورة، لا تحميل بطيء

### 4.13 منطق منع التكرار
- Request Deduplication: نفس الطلب لا يُرسل مرتين
- Notification Deduplication: نفس الإشعار لا يظهر مرتين
- Toast Deduplication: نفس الرسالة لا تظهر مرتين
- Order Deduplication: نفس العملية لا تُنفَّذ مرتين

### 4.14 منطق Network Manager
- مراقبة الاتصال وجودة الشبكة
- كشف Offline واستعادة تلقائية
- إعادة مزامنة عند عودة الإنترنت
- تحسين جميع الطلبات للعمل بأقل سرعة ممكنة

### 4.15 منطق حذف المستخدم نهائياً
- حذف من: Auth، Database، Profiles، Sessions، Notifications، Activity، Logs
- إذا كان المستخدم داخل التطبيق: عرض رسالة تم حذف حسابك بواسطة الإدارة. ثم تسجيل خروج فوري

### 4.16 منطق نظام الحظر
- المحظور لا يستطيع تسجيل دخول ولا إنشاء Session
- عرض رسالة: تم حظر حسابك بواسطة الإدارة.

### 4.17 منطق نظام الإشعارات
- فحص مكان التخزين (Supabase/Local)
- نظام تنظيف تلقائي: حذف إشعارات أقدم من 30 يوماً للمستخدمين
- منع تكرار الإشعارات
- كل مستخدم جديد يبدأ بإشعاراته الخاصة فقط

### 4.18 منطق أزرار النسخ
- زر Copy بجانب: اسم المستخدم، User ID، رقم الهاتف، البريد، رقم العملية، Transaction ID
- عند الضغط: نسخ النص إلى الحافظة

### 4.19 منطق إعادة بناء لوحة الإدارة
- حذف جميع Popup/Modal/Dialog المتعلقة بإدارة المستخدمين
- استبدالها بصفحات كاملة (Full Screen Pages)
- كل صفحة تحتوي على: Header واضح، Breadcrumb، محتوى منظم، Sticky Action Bar
- Animations احترافية عند التنقل بين الصفحات
- Loading States و Skeleton Loading
- Confirmation Dialogs للإجراءات الحساسة
- Success/Error Messages واضحة
- الحفاظ على الهوية البصرية الحالية والثيم
- لا تغيير على أي منطق موجود (API، قاعدة البيانات، المصادقة، Edge Functions)
- الحفاظ على جميع الوظائف الحالية وعدم كسر أي جزء

### 4.20 منطق Application Version Center
- تخزين معلومات الإصدارات في قاعدة البيانات
- تحديث معلومات الإصدار عند إصدار نسخة جديدة
- مقارنة إصدار المستخدم مع آخر إصدار متاح
- إجبار المستخدم على التحديث إذا كان Force Update مفعلاً
- منع المستخدم من الدخول إذا كان إصداره محظوراً
- منع المستخدم من الدخول إذا كان إصداره أقل من الحد الأدنى

### 4.21 منطق Live Monitoring
- تسجيل كل اتصال للمستخدم
- تحديث حالة المستخدم (Online/Offline) تلقائياً
- تسجيل آخر نشاط لكل مستخدم
- تسجيل الإصدار المستخدم
- تسجيل الجهاز المستخدم
- تسجيل آخر API Request
- تسجيل آخر Runtime Config
- تسجيل آخر Feature Flags
- تحديث البيانات كل 10 ثوانٍ

### 4.22 منطق Feature Management
- تخزين حالة كل ميزة في قاعدة البيانات
- تحميل حالة الميزات عند فتح التطبيق
- تحديث حالة الميزات من الخادم كل 30 ثانية
- إخفاء الميزة المعطلة من واجهة المستخدم
- منع تنفيذ الميزة المعطلة
- تطبيق التغيير فوراً على جميع المستخدمين المتصلين
- تسجيل كل تعديل في سجل النظام

### 4.23 منطق تحسين تجربة الاستخدام
- Animations ناعمة عند التنقل بين الصفحات
- Loading States واضحة
- Skeleton Loading أثناء تحميل البيانات
- Confirmation Dialogs للإجراءات الحساسة
- Success Messages عند نجاح الإجراء
- Error Messages واضحة عند فشل الإجراء
- Search أسرع مع Debounce
- Pagination لتحسين الأداء
- Filters متعددة
- Sorting حسب عدة معايير
- Sticky Header عند التمرير
- Sticky Action Bar عند التمرير
- Responsive Design لجميع الأجهزة
- Mobile First

### 4.24 منطق تقييم الكروت واقتراح التعديلات (جديد)

#### 4.24.1 عرض زر التقييم
- يظهر زر تقييم الكارت / اقتراح تعديل في موضعين:
  - بعد نجاح عملية الشحن مباشرة
  - بجانب كل عملية ناجحة في سجل العمليات
- الزر يظهر فقط إذا كانت ميزة تقييم الكروت مفعلة في Feature Management
- إذا تم إيقاف الميزة: يختفي الزر فوراً من جميع المستخدمين

#### 4.24.2 نافذة التقييم
- عند الضغط على زر التقييم: فتح نافذة منبثقة (Popup)
- عرض معلومات العملية: رقم العملية، نوع الكارت، التاريخ
- حقول الإدخال:
  - عدد الوحدات الفعلية (حقل رقمي، اختياري)
  - السعر المخصوم الفعلي من فودافون كاش (حقل رقمي، اختياري)
  - أيام الصلاحية الفعلية (حقل رقمي، اختياري)
- زر رفع سكرين شوت:
  - اختياري
  - يسمح برفع صورة واحدة فقط
  - الصيغ المدعومة: PNG, JPG, JPEG
  - الحد الأقصى لحجم الملف: 5 MB
  - عرض الصورة بشكل مصغر بعد الرفع
  - إمكانية حذف الصورة قبل الإرسال
- زر إرسال التقييم
- زر إلغاء

#### 4.24.3 إرسال التقييم
- التحقق من صحة البيانات المدخلة
- رفع السكرين شوت إلى Supabase Storage (إن وجد)
- حفظ التقييم في قاعدة البيانات مع:
  - User ID
  - اسم المستخدم
  - رقم العملية
  - نوع الكارت
  - تاريخ العملية
  - تاريخ التقييم
  - البيانات المقترحة (الوحدات، السعر، الصلاحية)
  - رابط السكرين شوت (إن وجد)
  - حالة التقييم: جديد
- عرض رسالة نجاح
- إغلاق النافذة

#### 4.24.4 إدارة التقييمات (Admin)
- عرض جميع التقييمات في صفحة Card Feedback
- فلترة وترتيب التقييمات
- عرض تفاصيل كل تقييم
- عرض السكرين شوت بحجم كامل عند الضغط عليه
- تغيير حالة التقييم: جديد، قيد المراجعة، تم التطبيق، مرفوض
- إضافة ملاحظات Admin على التقييم
- حذف تقييم
- تصدير التقييمات

#### 4.24.5 Feature Flag
- إضافة مفتاح تحكم في Feature Management: ميزة تقييم الكروت
- عند تفعيل الميزة: يظهر زر التقييم لجميع المستخدمين
- عند إيقاف الميزة: يختفي زر التقييم فوراً من جميع المستخدمين
- التطبيق فوري بدون الحاجة لإعادة تحميل الصفحة

---

## 5. الحالات الاستثنائية والحدود

| الحالة | السلوك المتوقع |
|--------|----------------|
| إدخال كود تفعيل خاطئ | عرض رسالة خطأ، عدم السماح بالمتابعة |
| إدخال كود مستخدم مسبقاً | عرض رسالة الكود مستخدم من قبل، عدم السماح بالمتابعة |
| إدخال كود معطل | عرض رسالة الكود غير صالح، عدم السماح بالمتابعة |
| انتهاء الاشتراك | منع الشحن، عرض شاشة التفعيل، إرسال إشعار |
| محاولة الشحن بدون اشتراك نشط | عرض رسالة يجب تفعيل الاشتراك أولاً |
| إدخال رقم هاتف غير صحيح | عرض رسالة خطأ، عدم السماح بالمتابعة |
| عدم اختيار كارت | عرض رسالة يرجى اختيار الكارت |
| عدم توفر كروت (مصدر البيانات فارغ) | عرض رسالة لا توجد كروت متاحة حالياً |
| فشل عملية الشحن | تسجيل العملية بحالة فشل، عرض رسالة خطأ، عدم عرض زر التقييم |
| محاولة وصول مستخدم عادي للوحة الأدمن | رفض الوصول، عرض رسالة غير مصرح |
| محاولة Admin الوصول لصلاحيات Super Admin | رفض الوصول، عرض رسالة غير مصرح |
| عدم توفر بيانات للإحصائيات | عرض رسالة لا توجد بيانات متاحة |
| رفع صورة بصيغة غير مدعومة | رفض الرفع، عرض رسالة الصيغة غير مدعومة |
| رفع صورة أكبر من الحد المسموح | رفض الرفع، عرض رسالة حجم الملف كبير جداً |
| فشل رفع الصورة إلى Supabase Storage | عرض رسالة خطأ، عدم تحديث قاعدة البيانات |
| فشل تحديث قاعدة البيانات بعد رفع الصورة | عرض رسالة خطأ، حذف الصورة من Storage |
| محاولة مستخدم عادي الوصول لأدوات المطورين | إخفاء الأدوات، عدم عرضها في الواجهة |
| الضغط على بطاقة شبكة في صفحة الشبكات | الانتقال إلى صفحة الشبكة المحددة مع Animation ناعم |
| عدم توفر شعار شبكة | عرض شعار افتراضي أو نص اسم الشبكة |
| الضغط على زر العودة في صفحة شبكة | العودة إلى صفحة الشبكات مع Animation ناعم |
| محاولة شحن كارت بحالة maintenance/development/unavailable | لا يُرسل أي API، عرض Dialog: تم إيقاف هذا المنتج مؤقتاً، وسيعود للعمل فور توفره. |
| محاولة شحن كارت بحالة disabled_execution | الكارت ظاهر لكن التنفيذ معطّل، عرض نفس الـ Dialog |
| محاولة إرسال نفس الطلب مرتين | منع الإرسال، عرض رسالة الطلب قيد التنفيذ |
| محاولة عرض نفس الإشعار مرتين | منع العرض |
| محاولة عرض نفس Toast مرتين | منع العرض |
| محاولة تنفيذ نفس العملية مرتين | منع التنفيذ |
| فقدان الاتصال بالإنترنت | عرض رسالة لا يوجد اتصال، تحميل البيانات من التخزين المحلي |
| عودة الاتصال بالإنترنت | إعادة مزامنة تلقائية |
| حذف مستخدم من لوحة الإدارة | حذف من جميع الجداول، إذا كان داخل التطبيق: عرض رسالة وتسجيل خروج فوري |
| محاولة مستخدم محظور تسجيل الدخول | رفض الدخول، عرض رسالة تم حظر حسابك بواسطة الإدارة. |
| مستخدم جديد يفتح صفحة الإشعارات | عرض إشعاراته الخاصة فقط، لا يرى إشعارات قديمة |
| Admin يضغط على مستخدم في قائمة المستخدمين | الانتقال إلى صفحة تفاصيل المستخدم |
| Admin يضغط على زر الإجراءات | الانتقال إلى صفحة إجراءات المستخدم |
| Admin يضغط على زر إدارة الاشتراك | الانتقال إلى صفحة إدارة الاشتراك |
| Admin يضغط على زر تعديل العمليات | الانتقال إلى صفحة تعديل العمليات |
| Admin ينفذ إجراء حساس | عرض Confirmation Dialog |
| نجاح تنفيذ إجراء | عرض Success Message، تسجيل الإجراء في السجل |
| فشل تنفيذ إجراء | عرض Error Message، تسجيل الخطأ في السجل |
| مستخدم يفتح التطبيق بإصدار محظور | منع الدخول، عرض رسالة يرجى تحديث التطبيق |
| مستخدم يفتح التطبيق بإصدار أقل من الحد الأدنى | منع الدخول، عرض رسالة يرجى تحديث التطبيق |
| Force Update مفعل ومستخدم بإصدار قديم | إجبار المستخدم على التحديث، عرض رسالة يجب تحديث التطبيق |
| Admin يعطل ميزة | إخفاء الميزة فوراً من جميع المستخدمين المتصلين |
| Admin يفعل ميزة | إظهار الميزة فوراً لجميع المستخدمين المتصلين |
| مستخدم يحاول الوصول لميزة معطلة | منع الوصول، عرض رسالة هذه الميزة غير متاحة حالياً |
| الضغط على زر تقييم الكارت | فتح نافذة التقييم |
| إرسال تقييم بدون إدخال أي بيانات | عرض رسالة يرجى إدخال بيانات واحدة على الأقل |
| رفع سكرين شوت بصيغة غير مدعومة | رفض الرفع، عرض رسالة الصيغة غير مدعومة |
| رفع سكرين شوت أكبر من 5 MB | رفض الرفع، عرض رسالة حجم الملف كبير جداً |
| فشل رفع السكرين شوت | عرض رسالة خطأ، السماح بإعادة المحاولة |
| نجاح إرسال التقييم | عرض رسالة نجاح، إغلاق النافذة |
| فشل إرسال التقييم | عرض رسالة خطأ، السماح بإعادة المحاولة |
| Admin يضغط على سكرين شوت في صفحة Card Feedback | عرض الصورة بحجم كامل |
| Admin يغير حالة تقييم | تحديث الحالة، تسجيل التعديل في السجل |
| Admin يحذف تقييم | حذف التقييم، حذف السكرين شوت من Storage |
| Admin يعطل ميزة تقييم الكروت | اختفاء زر التقييم فوراً من جميع المستخدمين |
| Admin يفعل ميزة تقييم الكروت | ظهور زر التقييم فوراً لجميع المستخدمين |

---

## 6. معايير القبول

1. المستخدم يفتح التطبيق ويشاهد شاشة البداية (Splash Screen) مع الشعار الديناميكي والنصوص
2. التطبيق يحمل البيانات الأساسية من التخزين المحلي أولاً
3. المستخدم ينتقل تلقائياً لشاشة التفعيل ويدخل كود تفعيل صحيح
4. النظام يفعل الاشتراك ويعرض الصفحة الرئيسية مع بيانات المستخدم والاشتراك الموسعة
5. المستخدم يشاهد كارت Premium عروض باقي الشبكات في الصفحة الرئيسية
6. المستخدم يضغط على كارت عروض باقي الشبكات وينتقل إلى صفحة الشبكات
7. المستخدم يشاهد 4 بطاقات Premium للشبكات (Vodafone، Orange، Etisalat، WE)
8. المستخدم يضغط على بطاقة Vodafone وينتقل إلى صفحة Vodafone
9. المستخدم يشاهد Header بشعار Vodafone ورسالة تحت التطوير
10. المستخدم يضغط على زر العودة ويعود إلى صفحة الشبكات
11. المستخدم يضغط على تبويب الشبكات في Bottom Navigation وينتقل إلى صفحة الشبكات
12. المستخدم يراجع صفحة الإحصائيات ويشاهد جميع البيانات الإحصائية والـ Charts الديناميكية
13. المستخدم لا يرى أي أدوات مطورين أو تصحيح في الواجهة
14. Admin يدخل لوحة الأدمن ويفتح قسم Product Config
15. Admin يعدل حالة كارت إلى maintenance
16. المستخدم يحاول شحن هذا الكارت ويشاهد Dialog: تم إيقاف هذا المنتج مؤقتاً، وسيعود للعمل فور توفره.
17. Admin يعدل سعر كارت
18. جميع المستخدمين يشاهدون السعر الجديد فوراً بدون تحديث APK
19. المستخدم يشاهد رسالة تنبيه قبل التنفيذ أسفل بيانات الكارت
20. المستخدم يضغط على زر الشحن
21. النظام يمنع إرسال الطلب مرتين
22. النظام ينتظر النتيجة النهائية قبل عرض الرسالة
23. إذا نجحت العملية: لا تظهر رسالة فشل أبداً، يظهر زر تقييم الكارت (إذا كانت الميزة مفعلة)
24. المستخدم يضغط على زر تقييم الكارت
25. النظام يفتح نافذة التقييم مع معلومات العملية
26. المستخدم يدخل البيانات المقترحة (الوحدات، السعر، الصلاحية)
27. المستخدم يرفع سكرين شوت (اختياري)
28. المستخدم يضغط على زر إرسال التقييم
29. النظام يحفظ التقييم في قاعدة البيانات
30. النظام يعرض رسالة نجاح ويغلق النافذة
31. المستخدم يفتح سجل العمليات
32. المستخدم يشاهد زر تقييم الكارت بجانب كل عملية ناجحة
33. المستخدم يضغط على زر التقييم من السجل
34. النظام يفتح نافذة التقييم بنفس الطريقة
35. Admin يفتح صفحة Card Feedback
36. Admin يشاهد جميع التقييمات المرسلة من المستخدمين
37. Admin يضغط على سكرين شوت
38. النظام يعرض الصورة بحجم كامل
39. Admin يغير حالة تقييم إلى قيد المراجعة
40. النظام يحدث الحالة ويسجل التعديل
41. Admin يفتح Feature Management
42. Admin يعطل ميزة تقييم الكروت
43. زر التقييم يختفي فوراً من جميع المستخدمين
44. Admin يفعل الميزة مرة أخرى
45. زر التقييم يظهر فوراً لجميع المستخدمين
46. المستخدم يفقد الاتصال بالإنترنت
47. التطبيق يعمل من التخزين المحلي بدون skeleton طويل أو صور مكسورة
48. المستخدم يستعيد الاتصال بالإنترنت
49. النظام يعيد المزامنة تلقائياً
50. Admin يحذف مستخدم نهائياً من لوحة الإدارة
51. النظام يحذف المستخدم من جميع الجداول
52. إذا كان المستخدم داخل التطبيق: يشاهد رسالة تم حذف حسابك بواسطة الإدارة. ثم تسجيل خروج فوري
53. مستخدم محظور يحاول تسجيل الدخول
54. النظام يرفض الدخول ويعرض رسالة تم حظر حسابك بواسطة الإدارة.
55. مستخدم جديد يفتح صفحة الإشعارات
56. المستخدم يشاهد إشعاراته الخاصة فقط، لا يرى إشعارات قديمة
57. Admin يضغط على زر Copy بجانب User ID
58. النظام ينسخ User ID إلى الحافظة
59. المستخدم يشاهد Footer في أسفل كل صفحة يحتوي على: Vodafone Fakka Premium، © 2026 Nader Akram، جميع الحقوق محفوظة
60. Admin يدخل لوحة الأدمن ويفتح قسم Users
61. Admin يشاهد قائمة المستخدمين بدون أي Popup أو Modal
62. Admin يضغط على مستخدم وينتقل إلى صفحة تفاصيل المستخدم الكاملة
63. Admin يشاهد جميع معلومات المستخدم والإحصائيات والسجلات في صفحة واحدة
64. Admin يضغط على زر الإجراءات وينتقل إلى صفحة إجراءات المستخدم
65. Admin يشاهد جميع الإجراءات مقسمة إلى مجموعات
66. Admin ينفذ إجراء تجديد الاشتراك
67. النظام يعرض Confirmation Dialog
68. Admin يؤكد الإجراء
69. النظام ينفذ الإجراء ويعرض Success Message
70. النظام يسجل الإجراء في سجل التعديلات
71. Admin يفتح صفحة Application Version Center
72. Admin يشاهد جميع معلومات الإصدارات وعدد المستخدمين على كل إصدار
73. Admin يفعل Force Update
74. مستخدم بإصدار قديم يفتح التطبيق
75. النظام يجبر المستخدم على التحديث
76. Admin يفتح صفحة Live Monitoring
77. Admin يشاهد جميع المستخدمين المتصلين في الوقت الفعلي
78. Admin يشاهد الإصدار المستخدم لكل مستخدم

---

## 7. الوظائف غير المشمولة في هذا الإصدار

- تكامل فعلي مع Recharge Engine أو API خارجي (سيتم لاحقاً)
- بوابات دفع لشراء الاشتراكات
- نظام إحالة أو عمولات
- تطبيق موبايل أصلي (iOS/Android)
- دعم لغات إضافية غير العربية
- نظام محادثة مباشرة مع الدعم
- تقارير تحليلية متقدمة
- تصدير البيانات بصيغ متعددة
- نظام نقاط أو مكافآت
- إشعارات Push خارج المتصفح
- مشاركة العمليات على وسائل التواصل
- نظام الأسئلة الشائعة FAQ
- دعم المحافظ الإلكترونية
- تعديل أي منطق موجود (API، قاعدة البيانات، المصادقة، Edge Functions)
- تعديل نظام الشحن الحالي
- حذف الأصول المرئية الحالية
- حذف كود أدوات المطورين
- تفعيل الخدمات في صفحات الشبكات (Vodafone، Orange، Etisalat، WE)
- إضافة أقسام الخدمات (فكة، دقائق، ميجابايت، باقات، كروت، عروض) في صفحات الشبكات
- تعديل الكروت الحالية في الصفحة الرئيسية
- تعديل صفحات المفضلة أو الإشعارات أو الاشتراكات
- تعديل Firebase أو قاعدة البيانات الحالية
- بناء APK (سيتم بعد انتهاء جميع التحديثات)
- تغيير الهوية البصرية أو الثيم الحالي
- تعديل تقييمات المستخدمين من قبل المستخدمين أنفسهم (التقييم يُرسل مرة واحدة فقط)
- إشعارات تلقائية للمسؤولين عند استلام تقييم جديد